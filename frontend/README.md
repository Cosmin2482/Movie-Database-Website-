# MovieDB Frontend (Angular 17)

- Auth (JWT) with interceptor
- Search, filter, sorting, pagination
- Movie details + reviews (posting requires login)

## Run
```bash
npm install
npm start
# http://localhost:4200
```
Proxy `/api` -> `http://localhost:5000`.
